import { HightlightKeywordsDirective } from './hightlight-keywords.directive';

describe('HightlightKeywordsDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightKeywordsDirective();
    expect(directive).toBeTruthy();
  });
});
